# Project Guidelines

- **Git History**: Do not force push or rewrite published git history.
- **Development**: Ensure all changes pass TypeScript compilation (`npm run build`) and ESLint checks (`npm run lint`).
- **State & Logic**: Keep adaptive signal control algorithms and simulation models deterministic and responsive to user inputs.

